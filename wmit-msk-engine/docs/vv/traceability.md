# V&V 추적표

ASME V&V 40 기반. 설계 문서 「OpenSim MSK 시뮬레이션 엔진 개발 프로세스 설계」 §6 을 따른다.

## 1차 COU (Context of Use)

> 설계 초기 단계에서 무릎 임플란트의 하중조건을 추정하는 **참고 도구**.

이 COU 를 전제로 모델 위험도를 낮게 두고, 그에 비례한 신뢰성 활동을 수행한다.
**인허가 제출 근거로 쓰려면 COU 를 재정의하고 V&V 범위를 확대해야 한다.**

## QoI (Quantity of Interest)

| 기호 | 양 | 단위 | 좌표계 |
| --- | --- | --- | --- |
| QoI-1 | 무릎 관절반력 (tibiofemoral) | N | 경골(tibia) |
| QoI-2 | 무릎 굴곡·신전 모멘트 | N·m | 경골 |
| QoI-3 | 주요 근육력 | N | — |
| QoI-4 | 관절가동범위 (ROM) | deg | — |

## 데이터 분리

검증 데이터를 모델 튜닝에 쓰면 검증이 아니다 (CLAUDE.md §4).
Grand Challenge trial 을 아래처럼 분리하고, 배정을 바꾸면 이 표를 먼저 고친다.

| 용도 | 피험자 / trial | Case 파일 | 비고 |
| --- | --- | --- | --- |
| 튜닝 | TBD | TBD | 마커 가중치·필터 설정 조정에만 사용 |
| 검증 | TBD | TBD | 계측 접촉력과 비교. 튜닝에 사용 금지 |

## 신뢰성 활동 추적

| ID | 구분 | 활동 | 합격기준 | 증거 위치 | 상태 |
| --- | --- | --- | --- | --- | --- |
| V-01 | 코드 검증 | 웹 파이프라인 결과 vs OpenSim GUI 수동 실행 (동일 입력·동일 Setup XML) | 주요 출력 수치 일치, 허용오차 TBD | `runs/`, 비교 노트 | 미착수 |
| V-02 | 계산 검증 | IK 마커 오차 (RMS·최대) | TBD — ADR-0003 | `provenance.json` 품질 지표 | 미착수 |
| V-03 | 계산 검증 | ID 잔차력·잔차모멘트 | TBD — ADR-0003 | `provenance.json` 품질 지표 | 미착수 |
| V-04 | 계산 검증 | 필터 차단주파수 민감도 | 영향 범위 보고 | 민감도 분석 결과 | 미착수 |
| V-05 | Validation | 계측 경골 접촉력 vs 예측 관절반력 | 파형 상관·RMSE, TBD — ADR-0003 | 검증 보고서 | 미착수 |
| V-06 | 불확실성 | Scaling·근육 파라미터·마커 오차의 출력 영향 | 주요 입력 ± 변동 시 출력 범위 보고 | UQ 결과 | 미착수 |
| V-07 | 적용성 | 검증 대상(TKA 고령 환자, 보행)과 사용 대상의 차이 | 적용 한계 문서화 | 본 문서 §적용 한계 | 미착수 |
| V-08 | 코드 검증 | 파이프라인-OpenSim 계약 (경로, 파일 이름, 해석 구간, 빈 결과) | 회귀 테스트 통과 | `tests/test_opensim_integration.py` | **완료** (토이 모델) |
| V-09 | 코드 검증 | 스케일링 항등성 — 모델 자신의 마커로 스케일하면 계수 1 | 계수 = 1 ± 1e-6 | `tests/test_device_integration.py` | **완료** (토이 모델) |
| V-10 | 코드 검증 | 기기 결합의 역학적 효과 (질량→모멘트 증가, 보조→근육력 감소, 토크 한계 준수) | 정성적 방향 일치 | `tests/test_device_integration.py` | **완료** (토이 모델) |
| V-11 | 계산 검증 | 기기 힌지 축과 인체 관절 축 정합성 | 허용 오차 내, 초과 시 경고 기록 | `provenance.json` 의 `hinge_misalignment_deg` | **완료** (점검 구현) |
| V-12 | Validation | 실제 데이터(Grand Challenge) 재현 | TBD — ADR-0003 | 검증 보고서 | 미착수 |

`TBD` 를 임의의 숫자로 채우지 않는다. 문헌 근거와 함께 ADR-0003 에서 확정한다.

## 적용 한계 (초안)

- 검증 데이터는 **계측형 인공관절을 이식한 고령 피험자의 보행**이다. 정상 무릎, 다른 동작, 다른 연령대로의 외삽은 검증되지 않았다.
- 근육력은 Static Optimization 의 비용함수 가정에 의존한다. EMG 로 확인하지 않았다.
- 개인화 수준은 신장·체중 기반 Scaling 이다. 영상 기반 개인 형상은 반영하지 않는다.
- **V-08 ~ V-11 은 토이 모델 기준이다.** 파이프라인이 OpenSim 을 올바르게
  부르는지와 결합이 역학적으로 작동하는지를 확인한 것이며, 해석 정확도나
  실제 보행 재현을 보인 것이 아니다. 그것은 V-12 의 몫이다.
- **기기 결합 한계** (ADR-0004): 기기는 인체 분절에 강체로 용접된다. 스트랩
  유연성·미끄러짐이 없으므로 전달 토크는 **상한**이다. 기기 내부 부재 하중은
  산출되지 않는다. 힌지 축 어긋남은 각도로 기록하되 그 구속력은 모델에 없다.
- `assistive` 모드의 기기 토크는 Static Optimization 비용함수가 정한 배분
  결과이며, 기기 요구사양이 아니다.

## 재현성

모든 실행은 `runs/<run_id>/provenance.json` 에 OpenSim 버전, Python 버전,
입력 파일 SHA-256, 설정 XML 사본, git commit 을 남긴다.
증거로 인용하는 결과는 반드시 이 기록과 함께 제시한다.
